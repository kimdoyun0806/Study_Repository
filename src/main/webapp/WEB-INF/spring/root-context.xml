<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- ===== 외부 프로퍼티 파일 로드 (DB 접속 정보) ===== -->
    <context:property-placeholder location="classpath:db.properties"/>

    <!-- ===== Component Scan: Service, DAO 빈 자동 등록 ===== -->
    <!-- Controller는 servlet-context.xml에서 등록 -->
    <context:component-scan base-package="com.hello">
        <context:exclude-filter type="annotation"
            expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- ===================================================
         PostgreSQL DataSource 설정
         db.properties의 pg.* 값 사용
    ==================================================== -->
    <bean id="postgresDataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="${pg.driver}"/>
        <property name="url"             value="${pg.url}"/>
        <property name="username"        value="${pg.username}"/>
        <property name="password"        value="${pg.password}"/>
        <property name="initialSize"     value="5"/>
        <property name="maxTotal"        value="20"/>
        <property name="maxIdle"         value="10"/>
        <property name="minIdle"         value="5"/>
        <property name="maxWaitMillis"   value="10000"/>
    </bean>

    <!-- ===================================================
         Oracle DataSource 설정
         ojdbc8.jar를 WEB-INF/lib 또는 Maven 로컬 저장소에 설치 후 활성화
         db.properties의 ora.* 값 사용
    ==================================================== -->
    <!--
    <bean id="oracleDataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="${ora.driver}"/>
        <property name="url"             value="${ora.url}"/>
        <property name="username"        value="${ora.username}"/>
        <property name="password"        value="${ora.password}"/>
        <property name="initialSize"     value="5"/>
        <property name="maxTotal"        value="20"/>
    </bean>
    -->

    <!-- ===== JdbcTemplate (PostgreSQL 기본 사용) ===== -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="postgresDataSource"/>
    </bean>

    <!-- ===== 트랜잭션 관리자 ===== -->
    <bean id="transactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="postgresDataSource"/>
    </bean>

    <!-- ===== @Transactional 어노테이션 활성화 ===== -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- ===================================================
         DDL Auto: 서버 시작 시 schema.sql 자동 실행
         Spring Boot의 ddl-auto와 동일한 역할
         schema.sql에 CREATE TABLE IF NOT EXISTS 사용 → 안전하게 중복 실행 가능
    ==================================================== -->
    <bean id="dataSourceInitializer"
          class="org.springframework.jdbc.datasource.init.DataSourceInitializer">
        <property name="dataSource" ref="postgresDataSource"/>
        <property name="databasePopulator">
            <bean class="org.springframework.jdbc.datasource.init.ResourceDatabasePopulator">
                <!-- schema.sql 경로 (src/main/resources/schema.sql) -->
                <property name="scripts">
                    <list>
                        <value>classpath:schema.sql</value>
                    </list>
                </property>
                <property name="continueOnError" value="true"/>
                <property name="sqlScriptEncoding" value="UTF-8"/>
            </bean>
        </property>
    </bean>

</beans>
